# ============================================
# 主机级 Nginx 配置 - 域名分发
# 将此文件放到服务器 /etc/nginx/conf.d/ 目录下
# 文件名建议: flytest-sites.conf
# ============================================

# etf.flytest.com.cn -> ai-trade 项目 (端口3080)
server {
    listen 80;
    server_name etf.flytest.com.cn;

    location / {
        proxy_pass http://127.0.0.1:3080;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 300s;
    }
}

# ai.flytest.com.cn -> ai-tools 项目 (端口6066)
server {
    listen 80;
    server_name ai.flytest.com.cn;

    location / {
        proxy_pass http://127.0.0.1:6066;  # ai-tools 项目的端口
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 300s;
    }
}
